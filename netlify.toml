[build]
  command = "cd web && bun install && bun run build"
  publish = "web/dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- web/"

[build.environment]
  NODE_VERSION = "22"
  BUN_VERSION = "latest"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200