{
  "buildCommand": "cd api && bun install && bun run build",
  "functions": {
    "api/dist/index.js": {
      "runtime": "bun",
      "includeFiles": "api/dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/skill", "destination": "/api/dist/index.js" }
  ]
}
