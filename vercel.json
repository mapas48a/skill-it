{
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- api/",
  "functions": {
    "api/index.ts": {
      "runtime": "bun"
    }
  },
  "rewrites": [
    { "source": "/api/skill", "destination": "/api/index.ts" }
  ]
}
