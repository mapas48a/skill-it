{
  "buildCommand": "bun install && bun run build",
  "functions": {
    "dist/index.js": {
      "runtime": "bun",
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/skill", "destination": "dist/index.js" }
  ]
}
